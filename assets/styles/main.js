import {StyleSheet} from 'react-native';

const globalStyle = StyleSheet.create({
  backgroundWhite: {
    backgroundColor: '#FFF',
  },
  flexStyle: {
    flex: 1,
  },
});

export default globalStyle;
